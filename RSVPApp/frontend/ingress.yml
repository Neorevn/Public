apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rsvp-ingress
  namespace: rsvp-app
spec:
  ingressClassName: nginx
  rules:
    - host: rsvp.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rsvp
                port:
                  number: 5000 # The Service port